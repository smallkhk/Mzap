{
  "name": "flash-sender-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Self-service panel for a single Flash Sender API key",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5175",
    "typecheck": "tsc --noEmit",
    "build:checked": "npm run typecheck && npm run build",
    "prebuilt": "rm -rf prebuilt && cp -r dist prebuilt"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
